{
  "name": "telemetry-station",
  "description": "Simple monitoring over serial or HTTP",
  "version": "0.0.0",
  "private": true,
  "author": {
    "name": "Jelle"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "launch": "npm run build && npm run publish",
    "publish": "npx gh-pages -d dist -r https://github.com/jellehak/telemetry-station"
  },
  "dependencies": {
    "vite-plugin-pwa": "^0.21.0",
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^5.4.11"
  }
}
